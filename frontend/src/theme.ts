import { createTheme } from '@mantine/core';

// Grayscale wireframe aesthetic: dark/gray primary, generous radius.
export const theme = createTheme({
  primaryColor: 'dark',
  fontFamily: 'Inter, system-ui, -apple-system, sans-serif',
  defaultRadius: 'md',
  cursorType: 'pointer',
});
